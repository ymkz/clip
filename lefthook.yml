pre-commit:
  parallel: true
  commands:
    format-ts-files:
      glob: '*.{ts,tsx}'
      run: prettier --write {staged_files} && eslint --fix {staged_files} && npm run lint:tsc
    format-other-files:
      glob: '*.{yml,json,html,md}'
      run: prettier --write {staged_files}
